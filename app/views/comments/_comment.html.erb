      <div class="comment">
            <div class="profile-picture">
                  <%= image_tag(comment.user.profile.profile_picture_path) %>
            </div>
            <div class="comment-text">
                  <div class="user-name"><%= comment.user.name %></div>
                  <div class="comment-body"><%= comment.body %></div>
            </div>
      </div>
